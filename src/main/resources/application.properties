spring.application.name=SkalmansFoodSleepClock

# Swagger UI / OpenAPI configuration
springdoc.api-docs.path=/v3/api-docs
springdoc.api-docs.enabled=true
springdoc.swagger-ui.path=/swagger-ui.html
springdoc.swagger-ui.enabled=true

# Datasource configuration (use environment variables first, fallback to local for dev)
spring.datasource.url=${JDBC_DATABASE_URL:jdbc:postgresql://dpg-d327e8ruibrs739kbj5g-a.frankfurt-postgres.render.com:5432/skalman_clock}
spring.datasource.username=${JDBC_DATABASE_USERNAME:nox}
spring.datasource.password=${JDBC_DATABASE_PASSWORD:APABP1gLidG9snW3FJddCrkqSHN43o8o}



# JPA / Hibernate Configuration

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=false

# OpenAI / Spring AI Configuration
spring.ai.retry.max-attempts=1
spring.ai.openai.api-key=${OPEN_AI_API_KEY}
spring.ai.openai.base-url=https://api.openai.com
spring.ai.openai.chat.options.model=gpt-4o
spring.ai.openai.chat.options.temperature=0.2
spring.ai.openai.chat.options.max-tokens=1000

# Multipart configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Server port (Heroku/Render assigns dynamic port)
server.port=${PORT:8080}
